pipeline {
    agent any  // This specifies that the pipeline can run on any available agent.

    stages {
        stage('Hello World') {
            steps {
                echo 'Hello, World!'  // This prints "Hello, World!" to the console.
            }
        }
    }
}
